@model IEnumerable<organiziranje.Models.skladiste_opreme>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.broj_na_polici)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.polica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.stalak)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.oprema.naziv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.skladiste.naziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.broj_na_polici)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.polica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.stalak)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.oprema.naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.skladiste.naziv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
